Theme files
-----------

Events (overwriteable):

wm.after_start()

Properties:

wm.starting_location(Rectangle)    -> { Rectangle, Point }  (inner window requested location is passed as a parameter,
                                                             expects outer window location and inner window position in outer window)


Global objects
--------------
theme
wm       (= theme.wm)
root     (link to graphic library)

Globals
-------

root:interface_name() : str